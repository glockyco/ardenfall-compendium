# Slice 1 — Item Walking Skeleton — Execution Progress

Durable, on-disk task tracker. Updated as each task completes. Source of truth for execution state; the plan describes the work, this file records what actually shipped and how it differed from the plan.

| Field            | Value                                                                                                                                                |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Plan**         | [`2026-05-03-item-walking-skeleton.md`](../plans/2026-05-03-item-walking-skeleton.md)                                                                |
| **Branch**       | `slice/1-item-walking-skeleton`                                                                                                                      |
| **Worktree**     | `.worktrees/slice-1-item-walking-skeleton/` (relative to repo root)                                                                                  |
| **Mode**         | [`skill://superpowers:subagent-driven-development`](skill://superpowers:subagent-driven-development) — fresh implementer + spec/code review per task |
| **Started**      | 2026-05-03                                                                                                                                           |
| **Phase status** | A 5/5 ✓ · B 5/5 ✓ · C 2/2 ✓ · D 5/5 ✓ · E 7/7 ✓ · F 4/4 ✓ · G 5/6 → · H 0/4 · I 0/3 · J 0/3 · K 0/2 · **total 33/46**                                |

## Status legend

- `○` pending
- `→` in progress (implementer dispatched, reviews pending, or fix-loop active)
- `✓` complete (spec compliance + code quality both green; commits on branch)
- `⚠` complete with caveats (recorded under Notes)

## Phase A — Repository bootstrap (5/5 ✓)

### A.1 Initialise Bun workspace + tsconfig.base · ✓

| Field           | Value                                                                  |
| --------------- | ---------------------------------------------------------------------- |
| Implementer     | `ef0490d` chore(repo): bootstrap bun workspace and base tsconfig       |
| Plan amendment  | `aacb3a2` fix(plan): document A.1 workspace member stubs               |
| Code-review fix | `9c0a4d7` fix(repo): add root tsconfig.json so bunx tsgo -p . resolves |
| Spec compliance | ✓ (controller-verified against amended plan)                           |
| Code quality    | ✓ (P1 fix re-reviewed)                                                 |

**Plan deviations:**

- Root `package.json`'s `"workspaces": ["pipeline", "site"]` field requires those directories to contain valid `package.json` files for `bun install` to succeed. Plan didn't anticipate this. Implementer added minimal stubs; controller authorized via IRC; plan amended (Step 5, file list, commit list) to document them. D.1 and H.1 will overwrite.
- The root `typecheck` script `bunx tsgo --noEmit -p .` had no `tsconfig.json` to load (only `tsconfig.base.json` existed). Code reviewer caught this as P1. Controller added a minimal root `tsconfig.json` (extends base, empty include) and amended the plan's A.1 to specify it.
- Local Bun was 1.3.10; plan pinned ≥1.3.13. Controller upgraded via `bun upgrade` — now on 1.3.13.

**Files committed (cumulative across all three commits):**

- `package.json`, `bun.lock`, `tsconfig.base.json`, `tsconfig.json`, `.editorconfig`, `.gitattributes`
- `pipeline/package.json`, `site/package.json` (stubs)
- Plan + roadmap amendments

### A.2 Configure Prettier and ESLint · ✓

| Field           | Value                                                                                      |
| --------------- | ------------------------------------------------------------------------------------------ |
| Pre-amendment   | `ec23334` fix(plan): stub site/svelte.config.js in A.2 (controller-issued before dispatch) |
| Implementer     | `8bc8c56` chore(repo): configure prettier and eslint flat config                           |
| Plan amendment  | `d3a5e86` fix(plan): A.2 step 5/6 reflect Prettier rewrites of A.1 files                   |
| Code-review fix | `35d6332` fix(eslint): ignore nested site/.svelte-kit/ output                              |
| Spec compliance | ✓ (controller-verified)                                                                    |
| Code quality    | ✓ (P2 fix re-reviewed)                                                                     |

**Plan deviations:**

- `eslint.config.js` imports `./site/svelte.config.js` at module evaluation time — a hard ES-module error until H.1 lands the real file. Pre-emptive plan amendment (`ec23334`) added a no-op `site/svelte.config.js` stub to A.2. H.1 overwrites.
- Prettier governs the whole repo from A.2 onward. `bun run format:check` failed on pre-existing A.1 config files (column-aligned JSON) and on authoritative docs (markdown with embedded JSON code fences that Prettier reformats by default). Implementer asked via IRC; controller authorized formatting **all** pre-existing files including docs. The implementer's commit included 8 Prettier rewrites alongside the 4 newly-created files; plan amended to reflect this scope (Step 5 runs `bun run format`, Step 6 commit list expanded).
- Code reviewer caught: ESLint flat-config bare `.svelte-kit/` ignore is root-only; SvelteKit emits to `site/.svelte-kit/`. Fixed to `**/.svelte-kit/`; plan code block updated to match.

**Note for future tasks:** the controller's incident log here had a serious controller mistake — I lost IRC context across turns and incorrectly accused the implementer of hallucinating "guidance from 0-Main"; reverted the commit before the user surfaced the IRC log proving it had happened. Restored `8bc8c56` via reflog. **Don't lose IRC history again.**

### A.3 Configure lefthook, .gitignore, LICENSE, README · ✓

| Field           | Value                                                                |
| --------------- | -------------------------------------------------------------------- |
| Implementer     | `e803368` chore(repo): add lefthook, gitignore, license, and readme  |
| Code-review fix | `770dbf2` fix(lefthook): pre-push test job uses --pass-with-no-tests |
| Spec compliance | ✓ (controller-verified)                                              |
| Code quality    | ✓ (P1 fix re-reviewed)                                               |

**Plan deviations:**

- Code reviewer caught: `bun test` in pre-push exits 1 when no tests are found (Bun 1.3.13 default). Fixed with `bun test --pass-with-no-tests` (Bun's documented escape hatch). Plan code block updated to match.

**Open caveats:**

- `dotnet format` glob `*.cs` matches no staged files until F.1+; safe until then.

### A.4 Repo-level AGENTS.md and CLAUDE.md · ✓

| Field           | Value                                                                                                                                                                   |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Implementer     | `61003d5` docs(repo): add agents.md orientation                                                                                                                         |
| Spec compliance | ✓ (controller-verified — 28-line orientation + 1-line redirect, byte-match)                                                                                             |
| Code quality    | ✓ (controller-verified; reviewer dispatch skipped — pure declarative content with no logic, abstractions, or test surface to evaluate; cost/value asymmetry documented) |

**Plan deviations:** none.

### A.5 GitHub Actions CI · ✓

| Field           | Value                                                             |
| --------------- | ----------------------------------------------------------------- |
| Implementer     | `8222b2b` ci(repo): add github actions workflow with path filters |
| Code-review fix | `499eb68` fix(ci): use dorny/paths-filter for PR change detection |
| Spec compliance | ✓ (controller-verified)                                           |
| Code quality    | ✓ (P1 fix re-reviewed)                                            |

**Plan deviations:**

- Implementer added `continue-on-error: true` to the `mod` job per the plan's own note at line 870 (csproj/Assembly-CSharp.dll not reachable until F.1; K.2 tightens). This was an in-scope plan modification, not a deviation.
- Code reviewer caught: `github.event.pull_request.changed_files` is the PR's numeric file count, **not** a list of paths. The `contains(toJson(...), 'pipeline/')` etc. expressions evaluated false on every PR. Pipeline/mod/fixtures jobs only fired on push to main; PRs got no pre-merge coverage. Fixed by adding a `changes` job using `dorny/paths-filter@v3` that emits boolean outputs per path group; affected jobs gate on `needs.changes.outputs.X == 'true'`. Plan A.5 yaml block AND K.2 mod-job yaml block both updated to match.

**Open caveats:**

- `site` job has `needs: pipeline` (no own filter). On a site-only PR, pipeline skips → site skips. K.2 may add a site filter; left intentionally for now.
- CI doesn't run yet — no remote configured. K.2's "Step 3" verifies green CI on a real push.

### Phase A gate · ✓

Verified 2026-05-03:

- `bun install --frozen-lockfile` exit 0 (152 installs, no changes)
- `bun run format:check` exit 0
- `bun run lint` exit 0
- `bun run typecheck` exit 0 (plan listed three local gates; we verified all four)
- `git status` clean

## Phase B — Schemas (5/5 ✓)

Five JSON Schema 2020-12 contracts plus Ajv standalone validators committed to `pipeline/dist/`. Cross-schema `$ref` between variant and entity verified to resolve at runtime.

| Task | Commit                                                                   | Status                                                       |
| ---- | ------------------------------------------------------------------------ | ------------------------------------------------------------ |
| B.1  | `eee5b4a` feat(schemas): define entity descriptor schema                 | ✓ implementer dispatch; spec compliance + structure verified |
| B.2  | `e133bf5` feat(schemas): define entity variant schema                    | ✓ controller-inline (declarative content)                    |
| B.3  | `5c0cdc` feat(schemas): define manifest and snapshot envelope            | ✓ controller-inline (declarative content)                    |
| B.4  | `7c59f64` feat(schemas): add digest stub and fixture manifest schemas    | ✓ controller-inline (declarative content)                    |
| B.5  | `5fc07ae` feat(pipeline): compile standalone validators with ajv 2020-12 | ✓ implementer dispatch + plan amendment `f20698a`            |

**Plan deviations:**

- B.5: plan said `import { standaloneCode } from "ajv/dist/standalone"` (named import). Installed ajv@8.20.0 publishes `standaloneCode` as **default** export (verified in `node_modules/ajv/dist/standalone/index.d.ts`). Plan amended (`f20698a`); implementer's adaptation is the correct form.
- B.5: implementer added `pipeline/dist/.gitkeep` (committed) and `.gitignore` carve-out section (`pipeline/dist/*` + negate `!validate-*.mjs`, `!.gitkeep`). All `git check-ignore` probes confirm carve-out works as intended.

**Process note:** B.2/B.3/B.4 were implemented controller-inline because the plan provides byte-literal JSON Schema content with no logic. Subagent dispatch overhead (~1m per task) was not justified for copy-paste of declarative spec. B.1 went through implementer dispatch (first schema task; established the pattern); B.5 went through implementer dispatch (substantive logic — codegen, dependency change, generated artifact commit). Cost/value rationale documented per task.

### Phase B gate · ✓

- `bun run codegen:validators` exit 0; six `pipeline/dist/validate-*.mjs` files produced
- `bun run typecheck` exit 0
- `ls pipeline/dist/validate-*.mjs` shows six files
- Cross-schema `$ref` validated at runtime (variant validator successfully delegates `field.name` validation to entity schema's pattern)

## Phase C — Item descriptors (2/2 ✓)

| Task | Commit                                                  | Status                                                     |
| ---- | ------------------------------------------------------- | ---------------------------------------------------------- |
| C.1  | `5823061` feat(entities): add item descriptor           | ✓ controller-inline; validated against entity schema       |
| C.2  | `6294a48` feat(entities): add item variants for slice 1 | ✓ controller-inline; all 5 validate against variant schema |

**Plan deviations:** none.

**End-to-end verification:** `validate-entity.mjs` returned `true` for `entities/item/entity.json`; `validate-variant.mjs` returned `true` for all five variant descriptors. This proves B+C agree: schemas describe shapes the descriptors actually use.

### Phase C gate · ✓

- All 6 entity descriptors parse as JSON
- All 6 validate against the standalone validators emitted in B.5

## Phase D — Pipeline foundation (5/5 ✓)

Pipeline workspace, types, registry, orchestrator, descriptor loader, snapshot loader, and validation stage. 11 tests across 4 files; cross-schema `$ref` resolution exercised end-to-end (descriptors + snapshots both validate against the standalone validators emitted in B.5). Codegen extended to emit `.d.mts` declarations alongside `.mjs` so consumers get proper TS types for `validate.errors`.

| Task | Commit                                                                   | Status                                                                             |
| ---- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- |
| D.1  | `f34de87` feat(pipeline): bootstrap workspace and shared types           | ✓ controller-inline; bundled with TS 7 / tsgo compatibility fixes (see deviations) |
| D.2  | `c551dbf` feat(pipeline): typed-map registry with duplicate detection    | ✓ controller-inline; 3 tests pass                                                  |
| D.3  | `5388dd2` feat(pipeline): topo-sorted stage orchestrator                 | ✓ controller-inline; 4 tests pass (Kahn's algorithm with cycle detection)          |
| D.4  | `1b4eb3b` feat(pipeline): descriptor loader stage with schema validation | ✓ controller-inline; codegen extended to emit `.d.mts`; 2 tests pass               |
| D.5  | `00845f8` feat(pipeline): snapshot loader and validation stages          | ✓ controller-inline; 2 tests pass; test assertion deviation documented             |

**Plan deviations:**

- D.1: plan A.1 specified `tsconfig.base.json` `"types": ["bun-types"]` but `bun-types` was never added to dependencies (it's not a real package; the canonical name is `@types/bun`). Plan amended (`680ff54`); A.1 package.json now lists `@types/bun ^1.3.13`; `tsconfig.base.json` switches to `"types": ["bun"]` (matches `@types/bun`'s `d.ts` entry).
- D.1: plan A.1 + D.1 + H.1 specified `"baseUrl": "."` in workspace tsconfigs. TS 7 (`tsgo`) **removes `baseUrl`** entirely. Plan amended (`680ff54`); workspace tsconfigs drop `baseUrl` and prefix `paths` entries with `./` (TS 7 also rejects bare non-relative paths). Same fix applied to H.1's site tsconfig in the plan body.
- D.4: plan's load-descriptors.ts cast validator-errors via `as unknown as { errors? ... }` because the `.mjs` modules had no declarations. Codegen now emits `.d.mts` with a typed `AjvValidateFunction` interface; the cast is gone. `.gitignore` extended to negate `pipeline/dist/validate-*.d.mts` so generated declarations stay tracked alongside the validators.
- D.5: plan's snapshot test asserted `result.errors).toEqual([])`. The synthetic fixture intentionally includes one non-fatal `lookupAssetGuidMissing` diagnostic on `fixture-leather-tunic.iconRef` (to exercise the missing-ref path). Test changed to `countsBySeverity.fatal === 0` plus "no `missingFatalField` codes". This is fixture-faithful; the plan's empty-errors expectation contradicted its own fixture design.

### Phase D gate · ✓

- `bun run codegen:validators` emits 6 `.mjs` + 6 `.d.mts` files
- `bun test pipeline/test` reports `11 pass, 0 fail` across 4 files
- `bun run --cwd pipeline typecheck` exits 0
- `bun run typecheck` (root) exits 0

## Phase E — Pipeline canonicalisation (7/7 ✓)

Descriptor-driven DDL emission, item canonicalisation with ancestor inheritance-layer inserts, site metadata emission, materialised read models, fast-check property invariants, and the end-to-end SQLite CLI. Pipeline now stands alone against the synthetic fixture: `bun run pipeline:run pipeline/test/fixtures/synthetic/snapshot pipeline/test/.tmp` produces a 152 KB SQLite blob with items, item_equipment, item_hand_items, item_primary_hand_items, item_melee_weapons, item_armor, item_tags, plus site metadata (site_entities, site_entity_fields, site_overview_columns, site_detail_sections, item_variants, site_read_models) and read models (item_overview_rows, item_detail_rows). 19 tests pass across 9 files (was 11 across 4 — added sql-ddl, canonicaliser, site-metadata, read-models, invariants/items).

| Task | Commit                                                                     | Status                                                                                |
| ---- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| E.1  | `1933150` feat(pipeline): build canonical ddl from descriptors             | ✓ controller-inline; 3 tests pass                                                     |
| E.2  | `0af0b0f` feat(pipeline): site metadata ddl                                | ✓ controller-inline; pure declarative SQL string                                      |
| E.3  | `c9fb713` feat(pipeline): item canonicaliser with ancestor variant inserts | ✓ controller-inline; 1 test pass; plan amendment `03e9f9b`                            |
| E.4  | `6d59c4d` feat(pipeline): emit site metadata from descriptors              | ✓ controller-inline; 1 test pass                                                      |
| E.5  | `828e75b` feat(pipeline): emit item overview and detail read models        | ✓ controller-inline; 1 test pass                                                      |
| E.6  | `920446e` test(pipeline): property invariants for item canonical ancestry  | ✓ controller-inline; 2 fast-check properties (50+30 runs) pass in <200ms              |
| E.7  | `3615144` feat(pipeline): emit-sqlite stage and pipeline run cli           | ✓ controller-inline; smoke run produces 151,552-byte SQLite; plan amendment `d960c50` |

**Plan deviations:**

- E.3 (commit `03e9f9b`): plan's `coerceForSqlite(value: unknown): unknown` is rejected by `bun:sqlite`'s `Statement.run(...args: SQLQueryBindings[])` because `unknown` is not narrow enough. Tightened return type to `string | number | null` (covers all values produced from the `null|boolean|number|string|object` discriminator). Plan's `row.variant` (typed `string | undefined` per `SnapshotRow`) was used as a SQLQueryBinding directly; replaced with `variant.variantId` since both are equivalent after the variant lookup succeeds.
- E.7 (commit `d960c50`): three TS-7 / `tsgo` defects in the plan literal:
  - Runtime imports of internal modules with `.ts` extension are rejected (only allowed on type-only imports under `verbatimModuleSyntax: true`). Existing pipeline files only used `.ts` on type-only imports because runtime cross-module imports didn't exist yet. Drop the extension; bundler module resolution finds the .ts via Bun.
  - emit-sqlite indexed `inputs["load-descriptors"].entities.item` etc. without guarding for `undefined`; under `noUncheckedIndexedAccess: true` these are `T | undefined`. Hoisted into named locals with a missing-fixture guard. Same pattern used in tests (snapshot.test.ts in D.5, canonicaliser.test.ts and read-models.test.ts in this phase).
  - cli's `runStages([loadDescriptors, loadSnapshot, validate, emitSqlite], {}, ctx)` requires a `Stage<unknown, unknown>[]` cast: under `strictFunctionTypes`, `Stage<I, O>` is contravariant in `I`, so `Stage<{"load-snapshot": …, "load-descriptors": …}, …>` is not assignable to `Stage<unknown, …>`. Existing orchestrator tests happen to compile because their lambdas have no parameters and `I` infers as `unknown`.
- E.6 used the plan's `arbItem` literal verbatim. fast-check 4.7's `uuid`, `float`, `option`, `constantFrom`, `record`, `array`, `asyncProperty`, `assert` APIs match the plan literal. Both invariants pass within 80 runs in ~150ms; well under the plan's 5 s budget.

**Process note:** all seven E.x tasks went controller-inline — they're plan-literal copy-paste with the deviations called out above. The implementer dispatch threshold was set in earlier phases (A.1/A.2/A.3/A.5/B.1/B.5 for tooling/bootstrap decisions) and Phase D established that pipeline implementation tasks fit the inline pattern. Phase E continues this. Code-quality review is controller-performed against the plan literal + verification gates; spec compliance is verified per-task by running the failing-then-passing test cycle the plan prescribes, then full gate (format, lint, typecheck root + workspace, full test suite).

### Phase E gate · ✓

Verified 2026-05-03:

- `bun test pipeline/test` reports `19 pass, 0 fail` across 9 files (`sql-ddl`, `canonicaliser`, `site-metadata`, `read-models`, `invariants/items` added; `registry`, `orchestrator`, `load-descriptors`, `snapshot` retained)
- `bun run pipeline:run pipeline/test/fixtures/synthetic/snapshot pipeline/test/.tmp` exits 0; emits `pipeline/test/.tmp/data.sqlite` (151,552 bytes — within plan's 32 KB / 1 MB envelope)
- inspection query `SELECT id, name, variant FROM item_overview_rows ORDER BY name` returns the two synthetic items
- `bun run format:check`, `bun run lint`, `bun run typecheck`, `bun run --cwd pipeline typecheck` all exit 0

## Phase F — Mod scaffolding + walker base (4/4 ✓)

BepInEx 5 csproj compiles cleanly against the live `Assembly-CSharp.dll` from the local Ardenfall Demo install. Walker base, ref resolver with missing-policy semantics, provenance helpers, shared DTOs, and the fail-fast preflight gate are all in place. `dotnet build` produces a clean `mod/bin/Debug/net472/ArdenfallArchives.dll` with **0 warnings, 0 errors**. APIs called against `Ardenfall.*` types were verified by ikdasm-disassembling Assembly-CSharp.dll and reading the IL signatures directly (commit `6eee2d8` records the resolved-API note for the next maintainer).

| Task | Commit                                                                            | Status                                                                                                |
| ---- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| F.1  | `b9aa388` feat(mod): bootstrap bepinex csproj and dll-copy helper                 | ✓ controller-inline; build clean; plan amendment `fae1bab` (nuget.config, BepInEx 5.4.21, net472)     |
| F.2  | `abd1399` feat(mod): shared snapshot dtos (ref, manifest, diagnostic, provenance) | ✓ controller-inline; build clean; plan amendment `ebd2d62` (Generic usings + IsExternalInit polyfill) |
| F.3  | `2223287` feat(mod): walker base, ref resolver, and provenance capture            | ✓ controller-inline; build clean; plan amendment `b80a2ee` (Generic usings + CS8604-clean null check) |
| F.4  | `149ea33` feat(mod): fail-fast preflight gate                                     | ✓ controller-inline; build clean; plan amendment `6eee2d8` (System/Linq usings + verified APIs)       |

**Plan deviations (each its own `fix(plan):` commit):**

- F.1 (`fae1bab`): three real defects — (a) BepInEx.Core lives on the BepInEx BaGet feed at `nuget.bepinex.dev/v3/index.json`, not nuget.org. Without `mod/nuget.config` adding the source, restore fails with NU1101. (b) BepInEx.Core 5.4.23 isn't published on the feed — highest 5.x stable is 5.4.21 (6.0.0-be.\* are IL2CPP-rewrite prereleases). Pinned to 5.4.21. (c) `<TargetFramework>net46</TargetFramework>` produces MSB3274 because Sirenix and (transitively) Assembly-CSharp target net471. Switched to net472 (Unity Mono ".NET 4.x Equivalent" scripting runtime); warnings vanish.
- F.2 (`ebd2d62`): two real defects — (a) `Manifest.cs` and `PreflightReport.cs` use `Dictionary<,>` and `List<>` without `using System.Collections.Generic;`. (b) all five DTOs use C# 9 `init` accessors. On net472 (which predates `IsExternalInit`) the compiler emits CS0518 unless we ship an internal polyfill type. Plan now lists `mod/src/Polyfill/IsExternalInit.cs` as a required Step 5b before commit.
- F.3 (`b80a2ee`): two real defects — (a) `RefResolver.cs` and `WalkerBase.cs` use `List<>`/`HashSet<>` without including `System.Collections.Generic`. (b) `string.IsNullOrEmpty(guid)` does not narrow `guid` to non-null in net472 reference assemblies (the `[NotNullWhen(false)]` attribute is missing); CS8604 fires on the next use. Replaced with `guid is null || guid.Length == 0` which the analyzer narrows correctly.
- F.4 (`6eee2d8`): three real defects — (a) `Preflight.cs` uses `DateTimeOffset`, `Func<>`, `Exception`, `.Any()`, `.All()` without `using System;` and `using System.Linq;`. (b) `BuiltLookupTable.GetAssetsOfType<T>` is a **static** method (per Demo2025 IL), not instance — calling through `Instance.GetAssetsOfType<T>()` is a method-resolution error. (c) `MasterRecordTable` has **no** `GetTables()` method; it exposes a public `Dictionary<string, RecordTableWrapper> tables` field directly. Replaced with `m.tables.Count > 0` and updated the plan caveat note to record the resolved API surface so future readers don't re-litigate.

**Verification approach for Ardenfall API references:** before writing C# that touches `Ardenfall.*` types, I disassembled `mod/libs/Assembly-CSharp.dll` with `ikdasm` and grep-read the IL for the exact member signatures (`get_Instance` for `BuiltLookupTable`, `instance string GetGuid([UnityEngine.CoreModule]UnityEngine.Object)` for the GUID lookup, public field shape of `WorldData.masterRecordTable`, `MasterRecordTable.tables`, etc.). This is faster and more reliable than ILSpy GUI for headless verification, and the resolved signatures land in the plan's caveat notes so the next reader doesn't have to repeat the disassembly.

**Process note:** all four F.x tasks went controller-inline with the established threshold. Bootstrap (F.1) had the most real-world plan defects because the plan's tooling expectations didn't match the BepInEx 5 ecosystem on macOS / .NET SDK 10. F.2/F.3/F.4 each had 2-3 omitted usings or wrong API references; the implementation work itself was plan-literal copy-paste once the deltas were identified.

### Phase F gate · ✓

Verified 2026-05-03:

- `dotnet build mod/ArdenfallArchives.csproj` exits 0 with **0 warnings, 0 errors**; produces `mod/bin/Debug/net472/ArdenfallArchives.dll`
- `dotnet format mod/ArdenfallArchives.csproj --verify-no-changes` exits 0
- `bun run format:check`, `bun run lint`, `bun run typecheck`, `bun run --cwd pipeline typecheck` all exit 0
- `bun test pipeline/test` reports `19 pass, 0 fail` across 9 files (no regression from Phase E)
- `git status` clean

## Phase G — Mod item extraction (5/6 →)

| Task | Commit                                                               | Status                                                                                                      |
| ---- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| G.1  | `d406a02` feat(mod): item per-layer snapshot dtos                    | ✓ controller-inline; build + dotnet format clean; plan amendment `475c8e3`                                  |
| G.2  | `102664b` feat(mod): item per-layer extractors                       | ✓ controller-inline; Demo2025 IL verified; plan amendment `75aae10`                                         |
| G.3  | `b2876f7` feat(mod): item extractor with variant dispatch            | ✓ controller-inline; static `BuiltLookupTable.GetAssetsOfType<T>`; plan amendment `4fd1075`                 |
| G.4  | `bad9f29` feat(mod): atomic snapshot writer and manifest builder     | ✓ controller-inline; build + dotnet format clean; plan amendment `c136973`                                  |
| G.5  | `cc6f462` feat(mod): plugin entry, hotkey trigger, readiness monitor | ✓ controller-inline; build + dotnet format clean; plan amendment `9e8f743`                                  |
| G.6  | —                                                                    | ⚠ manual F8 smoke retained as fallback; primary automation is paused until HotRepl control-plane plan lands |

**Plan deviations:**

- G.1 (`475c8e3`): `ItemSnapshot.cs` needs `using System.Collections.Generic;` for `Dictionary<,>` and `List<>`.
- G.2 (`75aae10`): Demo2025 IL verification corrected the descriptor/adapter contract. `ItemData.value` is actually `moneyValue`; equipment slots come from `usableSlots.Get()`; `HandItemData` contributes `animationSpeedMultiplier`; `PrimaryHandItemData` contributes `twoHanded`; melee fields are `damage`, `criticalHitChance`, `durabilityMax`, and `canBlock`; armor fields are `armorRating`, `durabilityMax`, and `usableSlots`-derived coverage. The duplicate descriptor field name `durabilityMax` was split into `meleeDurabilityMax` and `armorDurabilityMax` so site metadata has unique field ids per entity.
- G.3 (`4fd1075`): `BuiltLookupTable.GetAssetsOfType<T>()` is static in Demo2025, net472 nullable analysis needs `guid is null || guid.Length == 0`, and `KeyValuePair<,>` deconstruction is unavailable without extra support.
- G.4 (`c136973`): manifest/writer plan literals needed `System`, `System.Collections.Generic`, and `System.IO` usings; snapshot writes now create the base directory before staging and use sub-second timestamps to avoid same-second directory collisions.
- G.5 (`9e8f743`): plugin entry uses a `PreflightRunner` alias, registers the readiness monitor and console stub in `Awake`, disposes the scene-load subscription in `OnDestroy`, and includes required `System.*` usings for IO, LINQ, collections, and exception handling.

**Phase G.1-G.5 gate verified 2026-05-06:** `dotnet build mod/ArdenfallArchives.csproj -c Debug` exits 0 with 0 warnings/0 errors; `dotnet format mod/ArdenfallArchives.csproj --verify-no-changes` exits 0; `bun run format:check`, `bun run lint`, `bun run typecheck`, and `bun run --cwd pipeline typecheck` exit 0; `bun test pipeline/test` reports 19 pass, 0 fail, 119 expect() calls.

**Direction update 2026-05-06:** do not continue toward hardcoded in-game export orchestration. HotRepl will be improved first as a game-agnostic typed command/job/artifact control plane, then Ardenfall export automation will be rebuilt on top of that. New design: `docs/superpowers/specs/2026-05-06-hotrepl-export-workflow-design.md`. Upstream HotRepl plan: `/Users/joaichberger/Projects/HotRepl/docs/superpowers/plans/2026-05-06-hotrepl-control-plane.md`.

## HotRepl export automation setup

### Local BepInEx setup · ✓

Verified 2026-05-07 for the CrossOver Steam bottle game root:

`/Users/joaichberger/Library/Application Support/CrossOver/Bottles/Steam/drive_c/Program Files (x86)/Steam/steamapps/common/Ardenfall Demo/`

- Installed BepInEx 5.4.23.4 Windows x64 package; SHA-256 verified as `f881201b79da03e513bf97cdf39607ffa7f9e0d31a519b1aeeca8eb60f8309e7`.
- Created `BepInEx/plugins/` and `BepInEx/config/`.
- Persisted CrossOver Wine DLL override: `winhttp = native,builtin`.
- Launched `Ardenfall.exe` once via `cxstart --bottle Steam --dll winhttp=n,b`.
- Observed `BepInEx/LogOutput.log` with `Chainloader startup complete` and `0 plugins to load`.

### Mod target update · ✓

Verified 2026-05-07: implementing HotRepl `IControlCommandHandler` from the Ardenfall mod requires consuming `ValueTask<T>` from `HotRepl.Core`'s `netstandard2.1` control contracts. The `net472` target cannot consume that API surface. Ardenfall runs on Unity 2022, which supports .NET Standard 2.1, so `mod/ArdenfallArchives.csproj` now targets `netstandard2.1`; `dotnet build mod/ArdenfallArchives.csproj -c Debug` exits 0 with the control handlers compiling.

### Controller deployment workflow · ✓

The controller workspace now includes `controller:deploy` for copying HotRepl BepInEx runtime DLLs plus `ArdenfallArchives.dll` into the game's `BepInEx/plugins/` tree, and `controller:export` for the typed HotRepl export flow. README documents the build, deploy, and export smoke commands.

### HotRepl export automation verification · ✓

Static gates passed 2026-05-07:

- `dotnet build mod/ArdenfallArchives.csproj -c Debug` exits 0 with 0 warnings/0 errors and emits `mod/bin/Debug/netstandard2.1/ArdenfallArchives.dll`.
- `dotnet format mod/ArdenfallArchives.csproj --verify-no-changes` exits 0.
- `bun run format:check` reports all matched files use Prettier style.
- `bun run lint` exits 0.
- `bun run typecheck` exits 0.
- `bun test pipeline/test` reports 19 pass, 0 fail, 119 expect() calls.
- `bun test controller/test` reports 13 pass, 0 fail, 30 expect() calls.
- `/Users/joaichberger/Projects/HotRepl` test suite reports 124 pass, 0 fail.
- `/Users/joaichberger/Projects/HotRepl` BepInEx host build exits 0 with 0 warnings/0 errors.
- `/Users/joaichberger/Projects/HotRepl` MelonLoader host build exits 0 with 0 warnings/0 errors using the Ancient Kingdoms MelonLoader and Il2CppAssemblies paths.
- `/Users/joaichberger/Projects/HotRepl` `dotnet format HotRepl.slnx --verify-no-changes` exits 0.

Live smoke progress:

- HotRepl commit `0983b2b` fixes BepInEx manager-object destruction by setting `gameObject.hideFlags = HideFlags.HideAndDontSave` during `Awake()`. With BepInEx's global `HideManagerGameObject = false`, repeated `lsof -nP -iTCP:18590 -sTCP:LISTEN` polls show `wineserver` listening on `*:18590`, and repeated `nc -vz 127.0.0.1 18590` checks succeed after startup.
- HotRepl commit `9779dad` releases the exclusive control lease when a controller disconnects, so failed export attempts no longer poison later retries until game restart.
- `bun run controller:deploy -- --hotrepl-out /Users/joaichberger/Projects/HotRepl/src/HotRepl.BepInEx/bin/Debug/netstandard2.1 --mod-out ./mod/bin/Debug/netstandard2.1 --plugins "<Ardenfall Demo>/BepInEx/plugins" --bind-host 0.0.0.0 --token <local-token>` exits 0, copies HotRepl sidecars plus `ArdenfallArchives.dll`, and writes `BepInEx/config/hotrepl.bepinex.cfg`.
- Ardenfall Demo launched through CrossOver with `--dll winhttp=n,b`; `BepInEx/LogOutput.log` shows both `[Ardenfall Archives Extractor 0.1.0]` and `[HotRepl 2026.507.0]` loaded, Fleck reports `ws://0.0.0.0:18590`, and chainloader startup complete.
- The game starts in `MainMenu`. Invoking the `continue` button loads `world_Ardenfall` as the active scene and additively loads `map_overworld` plus nearby `cell_overworld_*` scenes. `archive.preflight` fails in the menu because `ArdenfallGame.instance` and `worldData` are not initialized, then passes after world load.
- `bun run controller:export -- --url ws://127.0.0.1:18590 --token <local-token> --output ./snapshots --pipeline-out ./pipeline/dist` completes the HotRepl export and pipeline run after world load. The successful run wrote snapshot `snapshots/snapshots/0.0.10.91-20260507-0833524466740`, validated it, and emitted `pipeline/dist/data.sqlite` (1,482,752 bytes).

## Phase H — Site bootstrap and design tokens (4/4 ✓)

| Task | Commit                                                               | Status                                                                                                          |
| ---- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| H.1  | `f6cbe7c` feat(site): bootstrap sveltekit static workspace           | ✓ workspace member with `tsconfig.json` extending `./.svelte-kit/tsconfig.json`; SPA mode (`ssr=false`)         |
| H.2  | `76080ae` feat(site): tailwind v4 design tokens                      | ✓ `app.css` `@theme inline` with Ardenfall amber palette; canonical `cn = twMerge(clsx(...))` in `lib/utils.ts` |
| H.3  | `f401acf` feat(site): add shadcn-svelte initial primitive set        | ✓ button, input, label, select, dialog, tabs, tooltip, separator; `npx shadcn-svelte@1.2.7` (Node only)         |
| H.4  | `ffc80ab` feat(site): sqlite store with site-meta and item accessors | ✓ `@sqlite.org/sqlite-wasm` main-thread; `vite optimizeDeps.exclude`; typed `site_*` accessors                  |

**Cross-cutting decisions:** swapped `sql.js-fts5` (unmaintained since 2021-01-06) for `@sqlite.org/sqlite-wasm@^3.53.0-build1` (official SQLite-team Wasm; ships FTS5). `bunx --bun shadcn-svelte` warns "unsupported runtime"; the registry CLI is officially Node 22+, so the workflow uses `npx`. `components.json` written directly because `init` is interactive and rejects `slate` (valid base colors are `neutral|stone|zinc|mauve|olive|mist|taupe`); picked `neutral`. ESLint 10's `preserve-caught-error` rule caught a real bug in `controller/src/deploy.ts:58` — fixed by passing `{ cause: error }`. Site uses vanilla `typescript@^6.0.3` instead of `--tsgo` because tsgo currently fails to resolve `<script module>` named exports through Svelte's `*.svelte` ambient declaration.

### Phase H gate · ✓

Verified 2026-05-06: `bun run --cwd site check` reports 0 errors / 0 warnings across 816 files; `bun run --cwd site build` produces `site/build/` via adapter-static.

## Phase I — Site item routes and rendering (3/3 ✓)

| Task | Commit                                                                  | Status                                                                        |
| ---- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| I.1  | `1db8b5f` feat(site): item routes hydrated from pipeline-emitted SQLite | ✓ `+layout.{ts,svelte}`, `+page.svelte`, `EntityTable.svelte`                 |
| I.2  | `1db8b5f`                                                               | ✓ `/items` overview route loads from `item_overview_rows`                     |
| I.3  | `1db8b5f`                                                               | ✓ `/items/[id]` detail with descriptor-driven `fieldList` + `custom` registry |

**Notes:** `EntityTable<T extends { id: string | number }>` constraint chosen over `Record<string, unknown>` (which fails for typed row interfaces lacking index signatures). `svelte/no-navigation-without-resolve` is scoped-disabled for `site/src/lib/components/ui/**/*.svelte` because shadcn primitives accept arbitrary user-supplied hrefs; one inline-disable in `EntityTable.svelte` for caller-supplied `rowHref`.

### Phase I gate · ✓

Verified 2026-05-06 against `pipeline/dist/data.sqlite` (1,482,752 bytes; 899 items): headless puppeteer smoke against `npx serve -s site/build -l 8090` returned `/items` with 899 rows and `/items/<id>` rendering an item detail with descriptor sections.

## Phase J — Fixtures and hygiene (3/3 ✓)

| Task | Commit                                                                             | Status                                                                                              |
| ---- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| J.1  | `dd01676` refactor(fixtures): move synthetic fixture under fixtures/ with manifest | ✓ `fixtures/synthetic/snapshot/{manifest,items}.json` + `fixtures/synthetic/manifest.json` envelope |
| J.2  | `a1104f1` feat(fixtures): hygiene check script with size budgets and manifests     | ✓ `pipeline/scripts/check-fixtures.ts` + `fixtures/real-capsule/.gitkeep`                           |
| J.3  | `4d8a0f3` feat(fixtures): pin synthetic hashes and add curation tool stub          | ✓ real sha256 hashes + `fixtures/scripts/curate-capsule.ts` stub                                    |

**Plan deviations:** `schemas/fixture-manifest.schema.json` had `additionalProperties: false` and the manifest example uses `$schema` for IDE autocomplete. Added `"$schema": { "type": "string" }` to the schema and regenerated `pipeline/dist/validate-fixture-manifest.mjs` so authoring `$schema` doesn't fail validation.

### Phase J gate · ✓

Verified 2026-05-07: `bun run check:fixtures` exits 0 with `fixtures ok: 2897 bytes total`; the `fixtures` CI job is wired in `.github/workflows/ci.yml` with `CI: "true"` env to enforce the forbidden-paths rule.

## Phase K — End-to-end and CI verification (2/2 ✓)

| Task | Commit                                                          | Status                                                                                                                                                                                                             |
| ---- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| K.1  | `7d8c001` test(pipeline): end-to-end synthetic smoke            | ✓ orchestrator-driven `loadDescriptors → loadSnapshot → validate → emitSqlite` over the synthetic fixture                                                                                                          |
| K.2  | `f87605e` ci(repo): wire site sqlite build and mod format check | ✓ site job runs `pipeline:run fixtures/synthetic/snapshot site/static` before `bun run --cwd site build`; mod job switches to `dotnet format --verify-no-changes` (no `Assembly-CSharp.dll` redistributable in CI) |

## Cross-cutting decisions made during execution

| Decision                                                                                  | When | Rationale                                                                                                                              |
| ----------------------------------------------------------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace member stubs (`pipeline/package.json`, `site/package.json`) ship in A.1         | A.1  | Bun workspace field requires real package files. D.1 and H.1 overwrite with full content.                                              |
| Root `tsconfig.json` (extends base, empty include) ships in A.1                           | A.1  | `bunx tsgo -p .` needs a project to load. Code reviewer caught the gap.                                                                |
| Local Bun upgraded to 1.3.13                                                              | A.1  | Plan pinned 1.3.13; local was 1.3.10. `bun upgrade` brought it to spec.                                                                |
| `site/svelte.config.js` stub ships in A.2                                                 | A.2  | `eslint.config.js` imports it; without a stub ESLint can't load its config.                                                            |
| Prettier rewrites of pre-existing A.1 files **and** authoritative docs ship in A.2 commit | A.2  | Prettier governs the whole repo from A.2; column-aligned JSON in A.1 and embedded JSON code fences in plan markdown all get rewritten. |
| `docs/` is intentionally **not** in `.prettierignore`                                     | A.2  | Plan code samples should match the repo's actual formatting; ignoring docs creates a coverage hole.                                    |
| ESLint ignore for `.svelte-kit/` uses `**/` prefix                                        | A.2  | Flat config requires `**/.svelte-kit/` for nested matching. SvelteKit emits to `site/.svelte-kit/`.                                    |

## Process notes

- **Plan amendments are commits, not edits-in-place-without-record.** Every plan deviation discovered during execution lands as a `fix(plan):` commit before the implementer dispatch runs.
- **Two-stage review per task** when the work has substantive content (logic, configs with semantics). For purely declarative bootstraps that the controller can self-verify, spec compliance is controller-performed and only code quality is delegated.
- **IRC is durable.** When an implementer DMs the controller, the exchange persists in their report and in the controller's per-turn IRC injection. Re-read implementer reports carefully before doubting them.
